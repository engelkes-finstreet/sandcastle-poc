export const Constants = {
  defaultPageSize: "20",
  // TODO(boilerplate): Replace with the actual company/bank name.
  companyName: "Beispiel Bank",
  passwordPattern: "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[^A-Za-z0-9]).{12,}$",
  // TODO(boilerplate): Replace with the actual Terms & Conditions URL.
  termsAndConditionsUrl: "https://google.com",
  // TODO(boilerplate): Replace with the actual imprint/legal notice URL.
  imprintUrl: "https://google.com",
};
